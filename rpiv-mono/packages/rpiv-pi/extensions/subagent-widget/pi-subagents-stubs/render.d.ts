/**
 * Compile-time stub for `pi-subagents/render` — see `./index.d.ts` for
 * the stub rationale.
 */

import type { Theme } from "@mariozechner/pi-coding-agent";
import type { Component } from "@mariozechner/pi-tui";

export declare function renderSubagentResult(result: unknown, options: { expanded: boolean }, theme: Theme): Component;
